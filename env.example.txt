# ===========================================
# VPN Telegram Bot - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp env.example.txt .env
# ===========================================

# ----- Application Mode -----
# "local" = Mock mode (no real Marzban API calls, fake VPN keys)
# "production" = Real mode (connects to actual Marzban server)
APP_ENV=local

# ----- Database -----
# Connection string for PostgreSQL
# For local Docker: postgres://vpnbot:vpnbot_password@localhost:5432/vpnbot?sslmode=disable
# For production: your remote PostgreSQL URL
DATABASE_URL=postgres://vpnbot:vpnbot_password@localhost:5432/vpnbot?sslmode=disable

# ----- Telegram Bot -----
# Get token from @BotFather
# BOT_TOKEN is set in config.yaml, but you can override it here if needed

# ----- Marzban VPN Panel (only needed for production) -----
# MARZBAN_URL=https://your-marzban-panel.com
# MARZBAN_USERNAME=admin
# MARZBAN_PASSWORD=your_password

